import 'package:flutter/material.dart';

@immutable
class PaddingConstant {
  const PaddingConstant._();
  static const paddingAll = EdgeInsets.all(12.0);
  static const paddingAllLow = EdgeInsets.all(8.0);
  static const paddingTopLeft = EdgeInsets.only(top: 16, left: 16);
}
